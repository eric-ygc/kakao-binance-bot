================================================
  작업내역 2026-03-27 00:00 ~ 22:32
================================================

1. 앵커 매칭 개선 (모니터링 코드 감지 실패 수정)
------------------------------------------------
- 문제: Selenium exe 교체 후 카카오톡 코드 감지 안 됨
- 원인: 클립보드 텍스트 길이가 매번 달라져 앵커 10줄 매칭 실패
- 수정: message_monitor.py
  · 앵커 축소 매칭: 10줄 실패 → 3줄 → 2줄 → 1줄 점진적 시도
  · 앵커 매칭 실패 시 스킵 대신 앵커 재설정

2. 프록시 설정 수정 안내
------------------------------------------------
- 문제: _streaming-1 (Sticky Session) 설정으로 모든 계정이 같은 IP 사용
- 원인: 잘못된 안내로 Sticky Session 사용 중이었음
- 해결: 프록시에서 _streaming-1 제거 → 요청마다 새 IP 할당

3. 대시보드 원격 스크린샷 기능 추가
------------------------------------------------
- 대시보드에서 "화면 캡처" 버튼 → PC 화면 스크린샷 표시
- 흐름: 대시보드 → 서버 → 에이전트(PIL.ImageGrab) → 서버 → 대시보드
- 수정 파일:
  · server/models.py — Screenshot 테이블 추가
  · server/routers/agent.py — 스크린샷 업로드 엔드포인트
  · server/routers/dashboard.py — 스크린샷 조회 엔드포인트
  · agent/agent.py — 화면 캡처 + 업로드 로직
  · server/static/app.js — 캡처 버튼 + 이미지 표시 UI
- 요구사항: 각 PC에 Pillow 설치 필요 (pip install Pillow)

수정된 파일
------------------------------------------------
- src/message_monitor.py — 앵커 축소 매칭 + 재설정
- server/models.py — Screenshot 모델
- server/routers/agent.py — 스크린샷 업로드 API
- server/routers/dashboard.py — 스크린샷 조회 API
- agent/agent.py — 스크린샷 캡처/업로드
- server/static/app.js — 화면 캡처 UI

4. 대시보드 코드 일괄 전송 기능 추가
------------------------------------------------
- 전체 현황 페이지에 코드 입력란 추가
- 온라인 PC 전체에 코드 동시 전송 (모니터링 불필요)
- 9자리 영숫자 코드 검증 포함
- 수정: server/static/app.js

5. 코드 브로드캐스트 API + Tasker 연동
------------------------------------------------
- 서버: POST /api/broadcast-code 엔드포인트 추가
- 대시보드: 전체 현황 페이지에 코드 일괄 전송 입력란 추가
- 모바일 연동: Tasker 앱으로 본채트 알림 감지 → 서버 API로 자동 전송
- 서버 기능: 발신자 "Stephen Beard" 필터 + 9자리 코드 자동 추출
- 본채트 모니터링 시간대:
  · 07:30~08:10 채팅방: BG-015 Wealth Sharing Investment Group
  · 23:30~23:59 채팅방: BG - Adjustment Team
- pickmob 폴더를 kakao-binance-bot 안으로 이동

6. 프록시 설정 수정
------------------------------------------------
- _streaming-1 (Sticky Session) 제거 → 요청마다 새 IP 할당

7. 새 PC 설치 (3번째 PC)
------------------------------------------------
- Python 3.12.10 설치 (py 명령어 사용)
- pip install requests Pillow
- 픽보조_Selenium.exe + agent.py + config 파일 배치
- agent_config.json 설정 (API 키 + 서버 URL)
- SSL 인증서 문제 → agent.py에 session.verify = False 추가
- Railway 도메인: .app이 정확 (.com 아님!)
- IPRoyal 프록시 공유 설정 (_streaming-1 없이)
- 총 3대 PC / 149계정 운영 시작

배포
------------------------------------------------
- GitHub push → Railway 자동 배포
- PC_01: 에이전트 재시작 필요 (Pillow 설치 완료)
- PC_02_독산: pip install Pillow + 에이전트 재시작 필요
