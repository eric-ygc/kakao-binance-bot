================================================
  작업내역 2026-03-26 15:00 ~ 16:10
================================================

1. ChromeDriver 버전 고정 (Chrome 146)
------------------------------------------------
- 문제: ChromeDriver 147 / Chrome 146 버전 불일치 → 세션 생성 실패
- 수정: browser_controller.py → uc.Chrome(options=options, version_main=146)
- 참고: Chrome 147 업데이트 후 제거 또는 147로 변경 필요

2. API/Selenium 빌드 완전 분리
------------------------------------------------
- 문제: Selenium exe 재빌드 시 curl_cffi 포함 → API 모드로 동작
- 수정:
  · app.py — _FORCE_SELENIUM 플래그 추가 (exe 이름에 "selenium" 포함 시 API 비활성)
  · version.py — VERSION_SELENIUM = "1.4.6" 추가
  · main.py — exe 이름 판별로 버전 표시 분리
- 결과: 픽보조.exe = API v1.5.6 / 픽보조_Selenium.exe = Selenium v1.4.6

3. 빌드 스크립트 생성
------------------------------------------------
- build.bat 추가
  [1] API 버전만 빌드       → dist\픽보조.exe
  [2] Selenium 버전만 빌드  → dist\Selenium백업(v1.4)\픽보조_Selenium.exe
  [3] 양쪽 다 빌드
  [4] 취소

4. 대시보드 site_urls 동기화 수정
------------------------------------------------
- 문제: 대시보드에서 사이트 URL 변경해도 PC 앱 GUI에 반영 안 됨
- 원인: _check_config_reload()에서 site_urls를 갱신하지 않았음
- 수정: app.py _check_config_reload()에 self._site_url_vars 업데이트 로직 추가

5. PC_01 프록시 "사용" 문제
------------------------------------------------
- 문제: 계정 proxy 필드에 "사용" 텍스트 → API에서 프록시 주소로 인식 → 전체 실패
- 원인: API 활성화 후 proxy 값을 실제로 읽게 되면서 발생
- 해결: PC_01 config.json에서 "proxy": "사용" → "proxy": "" 일괄 변경

수정된 파일
------------------------------------------------
- src/browser_controller.py — version_main=146
- app.py — _FORCE_SELENIUM, site_urls 리로드
- main.py — 버전 표시 분리
- version.py — VERSION_SELENIUM 추가
- build.bat — 빌드 스크립트 (신규)
