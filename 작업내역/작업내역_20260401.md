================================================
  작업내역 2026-04-01 00:00 ~ 05:00
================================================

1. pickmob 채팅방 자동 전환 개선
------------------------------------------------
- 뒤로가기 → 내 정보로 이동 문제 → 뒤로가기 1번만 + 추가 뒤로가기 제거
- 채팅방 클릭 성공 시 바로 진입 완료 처리 (반복 뒤로가기 방지)
- 메시지 탭 클릭 시도 제거 (채팅방 안에서는 안 보이므로)

2. pickmob 코드 중복 전송 방지
------------------------------------------------
- DEDUP_MS: 5초 → 1시간(3600000ms)으로 변경
- 같은 코드 1시간 내 재전송 안 함

3. pickmob 스케줄 자동 시작 (슬롯 5개 방식 적용)
------------------------------------------------
- RelayService.kt: checkScheduleAutoStart() 슬롯 5개 방식으로 수정
- 모니터링 시작 버튼 안 눌러도 슬롯 시간 되면 자동 시작
- 슬롯 시간 끝나면 자동 중지

4. 에이전트 버전 관리
------------------------------------------------
- agent.py에 AGENT_VERSION = "1.0.0" 추가
- heartbeat에 agent_version 전송
- 대시보드에서 앱 버전 + 에이전트 버전 둘 다 표시
- DB 마이그레이션 agent_version 컬럼 추가

5. 에이전트 commands.json 초기화 강화
------------------------------------------------
- 업데이트 후 앱 재시작 전 commands.json 비우기
- 이전 코드 자동 실행 방지

6. 픽보조모바일 버전 업그레이드
------------------------------------------------
- v1.4.1 → v1.4.2

수정된 파일
------------------------------------------------
- pickmob: MyAccessibilityService.kt, RelayService.kt, build.gradle
- kakao-binance-bot: agent/agent.py, server/models.py, server/database.py,
  server/routers/agent.py, server/routers/dashboard.py, server/static/app.js

대화 요약
------------------------------------------------
- pickmob 채팅방 자동 전환: 뒤로가기 → 내 정보 문제 → 뒤로가기 1번만으로 해결
- 코드 중복 전송: DEDUP 5초 → 1시간으로 해결
- 스케줄 자동 시작: 슬롯 5개 방식 적용, 버튼 안 눌러도 자동
- 에이전트 버전 관리: 대시보드에서 에이전트 버전 확인 가능
- Rocky Linux 10 클라우드 설정 가이드 제공 (DNF 패키지 관리자 추천)
- 코딩 규칙: 항상 버전업 + 한글 주석 자세히
