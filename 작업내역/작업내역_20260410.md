================================================
  작업내역 2026-04-10
================================================

1. pickmob 고정 메시지 코드 감지 문제 해결 (계속)
------------------------------------------------
- v1.5.7: Y좌표 상위 40% 무시 → 새 코드도 차단하는 문제 발생
- v1.5.8: pinnedCode 방식으로 전환
  - 채팅방 진입 후 첫 감지 코드 = 고정 메시지 → 저장 + 무시
  - 이후 다른 코드만 전송
- v1.5.9: pinnedCode 강화
  - pinnedCode 저장을 진입 10초 이내로 제한 (고정 메시지에 코드 없는 경우 대응)
  - processedCodes Set 추가 — 이미 전송한 모든 코드 중복 전송 차단
  - DEDUP 1시간 → 24시간
- 23:30 슬롯 성공 확인 ✅
- 07:30 슬롯 성공 확인 ✅ (자동입력으로 pickmob 정상 동작)

2. 대시보드 operator 계정 추가
------------------------------------------------
- OPERATOR_PASSWORD 환경변수 설정 (Railway)
- operator 계정: 코드 입력 + 전체전송만 가능
- PC 목록에서 이름 + 온라인 상태만 표시 (상세 정보 숨김)
- PC 클릭 불가 (상세 페이지 접근 차단)
- 관리 버튼 숨김 (PC 추가, 업데이트 배포, 계정 분배 등)
- 비밀번호: qwer1234

3. 코드 패턴 강화 (v1.6.0)
------------------------------------------------
- 문제: "555555555" (숫자만 9자리) 가 코드로 잘못 감지
- 원인: [A-Z0-9] 패턴이 숫자만으로 구성된 문자열도 매칭
- 수정: (?=[A-Z0-9]*[A-Z]) lookahead 추가 — 대문자 최소 1개 필수
- 영문만 9자리는 첫 글자 숫자 필수로 이미 차단됨

4. 새 프로젝트 논의
------------------------------------------------
- unikobulx.com 매크로 프로그램 개발 예정
- 별도 폴더에서 새로 시작
- 지침서 작성 후 진행

수정된 파일
------------------------------------------------
[kakao-binance-bot]
- server/config.py — OPERATOR_PASSWORD 추가
- server/auth.py — operator 역할, require_login, get_role
- server/routers/dashboard.py — operator 권한 제한
- server/static/app.js — operator UI 제한
- server/static/index.html — admin-only 클래스

[pickmob]
- MyAccessibilityService.kt — pinnedCode, processedCodes, DEDUP 24시간
- Constants.kt — 코드 패턴 대문자 필수
- build.gradle — v1.6.0

배포 정보
------------------------------------------------
- pickmob: v1.6.0 (설치 완료)
- 서버: Railway 자동 배포 완료 (operator 계정 포함)

현재 상태
------------------------------------------------
- pickmob 07:30 + 23:30 슬롯 정상 동작 확인
- 2일 모니터링 중 (안정화 확인)
- operator 계정 사용 가능 (qwer1234)
