================================================
  작업내역 2026-03-28 00:00 ~ 04:00
================================================

1. 프록시 "사용" 값 자동 무시 기능 추가
------------------------------------------------
- 문제: 대시보드 에이전트가 서버 config를 동기화하면서 PC에서 직접 수정한 proxy 값을 덮어씀
- 원인: 서버 DB에 "proxy": "사용" 값이 남아있어 에이전트가 계속 복원
- 수정: app.py에 _valid_proxy() 함수 추가 — URL 형식이 아닌 값 자동 무시
- 양쪽 exe 빌드 완료 (API + Selenium)

2. 대시보드 코드 입력란 값 유지
------------------------------------------------
- 문제: SSE 업데이트마다 renderOverview()가 호출되어 코드 입력란 초기화
- 수정: 렌더링 전 입력값 저장 → 렌더링 후 복원

3. 대시보드 설정 저장 후 스크롤 위치 유지
------------------------------------------------
- 문제: 저장 후 loadComputers() 호출로 화면 전체 재렌더링 → 스크롤 맨 위로
- 수정: 저장 후 loadComputers() 제거 → 스크롤 위치 유지

4. Tasker SSL 오류 수정
------------------------------------------------
- 문제: Tasker HTTP 요청 URL이 .com으로 설정되어 SSL 인증서 불일치
- 해결: .com → .app으로 URL 수정

5. 코드 전부 실패 (Invalid parameter) 분석
------------------------------------------------
- 23:30 타임 전계정 실패, 대시보드에 3대 PC 카운터 미표시
- 원인 분석: 프록시 "사용"은 Selenium에서 1파트라 무시됨 → 원인 아님
- 사이트 자체에서 코드가 유효하지 않았을 가능성 높음
- 카운터 미표시 원인: 자동입력 취소 시 카운트 집계 코드에 도달 못함

6. 자동입력 취소 시 카운트 집계 + 미실행 표시
------------------------------------------------
- 문제: 스케줄 종료로 자동입력 취소되면 성공/실패 카운트가 기록 안 됨
- 수정: 취소되어도 결과 집계 + 미실행 계정 수 표시
- 예시: 성공 6 / 실패 20 / 미실행 24 / 총 50 | 23:59:01

7. 로그에 시간 표시 추가
------------------------------------------------
- 자동 입력 시작, 재시도 시작, 전체 완료 시점에 HH:MM:SS 표시

8. 버전 업그레이드
------------------------------------------------
- API: v1.5.6 → v1.5.7
- Selenium: v1.4.6 → v1.4.7

9. 새 PC 설치 관련
------------------------------------------------
- PC_03, PC_04 (독산): 대시보드에서 프록시 "사용" 삭제 완료
- IPRoyal 프록시 공유 — Selenium에서도 인증 프록시 정상 동작 확인
- 새 exe 미설치 PC도 대시보드에서 프록시 삭제하면 정상 작동

수정된 파일
------------------------------------------------
- app.py — _valid_proxy(), 취소 시 카운트 집계, 시간 표시 추가
- version.py — v1.5.7 / v1.4.7
- server/static/app.js — 코드 입력란 값 유지 + 스크롤 위치 유지

10. pickmob 앱 서버 URL 수정
------------------------------------------------
- Constants.kt 서버 URL: .com → .app 수정
- Tasker 대신 pickmob 사용 결정 (접근성 서비스로 더 정확한 감지)
- 안 쓰는 폰에 본채트 + pickmob 설치 예정
- Android Studio에서 APK 빌드 필요

대화 요약
------------------------------------------------
- 23:30 타임 전계정 Invalid parameter 실패 → 사이트 쪽 문제로 추정
- 대시보드에서 PC_01 계정 프록시 "사용" 삭제 필요 발견
  → PC에서 직접 수정해도 에이전트가 서버 값으로 덮어씀
  → 대시보드에서만 수정해야 영구 반영됨
- 대시보드 코드 입력란이 SSE 업데이트로 계속 지워지는 문제 수정
- 계정 관리 저장 시 스크롤 맨 위로 올라가는 문제 수정
- Tasker 본채트 연동: SSL 오류(.com→.app) 수정, 수동 테스트 성공
- Tasker 자동 전송 + 수동 전송 중복해도 앱에서 중복 코드 무시됨
- 서버 수정은 Railway 배포만 하면 끝, 앱 수정은 PC에 exe 복사 필요
- pickmob으로 전환 결정: Tasker 알림 방식 한계 → 접근성 서비스로 정확한 감지
- 안 쓰는 폰에 본채트 + pickmob 설치 완료
- API 버전 딜레이 5초 고정 (2~3초 → 5초)
- API 버전 앱 내 코드 수동 입력 기능 추가 (대시보드 없이 독립 운영)
- 중지 버튼 누르면 재시도 건너뛰고 즉시 정지하도록 수정
- 대시보드에서 My PC(API 버전) 삭제 — API 버전 독립 운영 전환
- 시간대별 다른 계정 사용 기능 검토 중 (07:30 A계정, 23:30 B계정)
