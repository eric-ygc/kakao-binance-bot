================================================
  작업내역 2026-03-29 08:00 ~ 10:26
================================================

1. pickmob(픽보조모바일) UI 간소화
------------------------------------------------
- 불필요한 기능 제거: 사이트 URL, 계정 관리, 보너스픽 예약, WebViewActivity
- 남긴 기능: 모니터링 설정, 수동 코드 입력, 캐치된 코드, 로그
- 입력 필드 잠금 해제 (읽기 전용 → 편집 가능)
- 수동 코드 입력: WebView 대신 서버 API로 전송하도록 변경

2. pickmob 슬롯 5개 확장 + 슬롯별 채팅방/발신자
------------------------------------------------
- 기존 슬롯 2개 → 5개로 확장
- 각 슬롯에 시간(시작/종료) + 채팅방 이름 + 발신자 이름 입력
- SettingsPreferences.kt 전면 수정 (SlotSettings 데이터 클래스)
- 시간 입력 자동 포맷: 0730 → 07:30

3. 시간대별 자동 채팅방 전환
------------------------------------------------
- 슬롯 변경 감지 시 해당 채팅방으로 자동 이동 (본채트 내)
- MyAccessibilityService.kt: findActiveSlotWithIndex() 추가
- 슬롯별 발신자 필터 적용

4. IPRoyal 프록시 IP 회전 테스트
------------------------------------------------
- 5회 요청 → 5개 다른 IP 확인 → 정상 작동
- "Frequent requests" 원인은 IP가 아닌 다른 요인 (패턴/속도)
- 딜레이 5초 유지, 테스트 후 조절 예정

5. 원본 pickmob 폴더 정리
------------------------------------------------
- Android Studio가 원본 폴더(Desktop/Vibe Coding/pickmob)를 사용 중
- 원본 폴더의 Constants.kt, MainActivity.kt, activity_main.xml 수정
- WebViewActivity.kt 삭제

수정된 파일 (원본 pickmob 폴더)
------------------------------------------------
- Constants.kt — 서버 URL .app 수정, 더미 셀렉터 추가
- MainActivity.kt — UI 간소화, 슬롯 5개 동적 생성, 시간 자동 포맷
- MyAccessibilityService.kt — 슬롯 5개 지원, 자동 채팅방 전환
- SettingsPreferences.kt — SlotSettings 5개 슬롯 구조
- activity_main.xml — 불필요 섹션 제거, 동적 슬롯 컨테이너
- WebViewActivity.kt — 삭제

대화 요약
------------------------------------------------
- pickmob 앱 UI 대폭 간소화 (코드 추출 + 서버 전송만)
- 시간대별 다른 채팅방/발신자 설정 가능 (슬롯 5개)
- 시간대 변경 시 자동 채팅방 전환 기능 추가
- IPRoyal IP 회전 정상 확인 (매 요청 다른 IP)
- API 딜레이 5초 유지, 오늘 밤 23:30 첫 실전 테스트 예정
- 픽보조모바일 성공하면 수동 코드 추출 불필요 → 자동화 완성
