================================================
  작업내역 2026-04-03 00:00 ~ 01:30
================================================

1. 시스템모니터 위장 앱 생성
------------------------------------------------
- 가짜 시스템 모니터 UI (CPU/메모리/디스크/네트워크)
- Ctrl+Shift+P → 비밀번호 입력 → 진짜 픽보조 화면
- config.json 비밀번호 암호화 (XOR + base64)
- agent_config.json API 키 자동 암호화 (첫 실행 시)
- 보안 에이전트(시스템서비스.exe) 별도 빌드

2. 서버: 5분 이상 오래된 submit_code 명령 자동 만료
------------------------------------------------
- 이전 코드로 실행되는 문제 방지
- heartbeat 시 오래된 pending 명령 expired 처리
- 실행 중인 PC 작업에는 영향 없음

3. pickmob 코드 리뷰 반영
------------------------------------------------
- saveRelayConfig() 중복 호출 제거
- 미사용 urlFields 변수 삭제
- AndroidManifest.xml: allowBackup=false, usesCleartextTraffic=false, WebViewActivity 제거

4. pickmob 감시중 로그 빈도 줄이기
------------------------------------------------
- diagLogCount % 2 → % 30 (약 30초에 1번)

5. CodeGPT 설치 + 코드 리뷰
------------------------------------------------
- VSCode에 CodeGPT 확장 설치
- OpenAI API 키 연동
- AndroidManifest.xml, MainActivity.kt, MyAccessibilityService.kt, RelayService.kt 리뷰

수정된 파일
------------------------------------------------
- pickmob: MainActivity.kt, MyAccessibilityService.kt, AndroidManifest.xml, build.gradle
- server/routers/agent.py
- 시스템모니터_원거리: system_monitor.py, crypto_config.py, agent_secure.py

대화 요약
------------------------------------------------
- 시스템모니터 위장 앱 완성 (가짜 UI + 암호화 config + 보안 에이전트)
- PC들이 이전 코드로 실행되는 문제 → 서버 5분 만료 규칙으로 해결
- pickmob 23:30 타임 코드 감지 + 서버 전송 2회 성공 확인
- CodeGPT 리뷰 결과 반영 (중복 코드, 미사용 변수, 보안 설정)
- 감시중 로그 빈도 줄이기 (로그 넘침 방지)
- 07:30 타임 실패 원인 불명 (로그 없음) → 로그 빈도 줄여서 해결 기대
