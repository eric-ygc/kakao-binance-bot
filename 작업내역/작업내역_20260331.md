================================================
  작업내역 2026-03-31 00:00 ~ 04:36
================================================

1. 앱 시작 시 commands.json 초기화
------------------------------------------------
- 문제: 시작 시간 되자마자 이전 코드로 자동 실행
- 수정: app.py 시작 시 commands.json 비우기

2. Selenium 재시도 딜레이 5초로 변경
------------------------------------------------
- 기존: Selenium 재시도 10초 → 5초로 변경
- API 재시도는 이미 5초

3. 버전 체계 변경
------------------------------------------------
- API: 1.5.9 → A.2.0.0
- Selenium: 1.4.9 → S.1.0.0

4. 대시보드 원격 exe 업데이트 배포 기능
------------------------------------------------
- 대시보드에서 다운로드 URL 입력 → 선택한 PC에 배포
- 에이전트가 exe 다운로드 → 앱 종료 → 교체 → 재시작
- 실패 시 자동 롤백
- GitHub CLI 설치 + 인증 → Release 자동 생성
- 첫 Release: S.1.0.0 업로드 + 배포 테스트 성공!

5. 대시보드 실시간 원격 제어 기능
------------------------------------------------
- 2초마다 스크린샷 자동 갱신
- 스크린샷 클릭 → 해당 좌표로 마우스 클릭 전송
- 좌클릭/더블클릭/우클릭 선택
- 키보드 입력 전송
- 에이전트: pyautogui로 실행

6. 에이전트 exe 재빌드
------------------------------------------------
- Pillow + pyautogui 포함 빌드
- Python 미설치 PC에서도 스크린샷 + 원격 제어 가능

수정된 파일
------------------------------------------------
- app.py — commands.json 초기화, 재시도 딜레이 변경
- version.py — A.2.0.0 / S.1.0.0
- agent/agent.py — update_exe, remote_click, remote_key 핸들러
- server/static/app.js — 업데이트 배포 모달, 원격 제어 UI
- server/static/index.html — 업데이트 모달 추가

7. 보너스픽 API 구현
------------------------------------------------
- API 엔드포인트: POST /api/app/second/share/user/follow
- Payload: {"shareId": "4294409423340421112"}
- 로그인 후 follow 요청 — Chrome 불필요
- API 우선, Selenium 폴백
- 계정 간 딜레이 5초
- 버전: A.2.0.1 / S.1.0.1

8. GitHub Release + 원격 업데이트 배포 테스트
------------------------------------------------
- private repo → 404 에러 → public으로 변경
- 한글 파일명 → URL 인코딩 문제 → 영문 파일명(PickBozo_Selenium.exe)으로 변경
- 앱 종료 실패(파일 사용 중) → taskkill 3회 재시도 + 프로세스 확인 로직 추가
- 최종 성공: 다운로드 → 앱 종료 → 백업 → 교체 → 재시작 (~4초)

9. 에이전트 exe 재빌드
------------------------------------------------
- update_exe, remote_click, remote_key 포함
- Pillow + pyautogui 포함

대화 요약
------------------------------------------------
- 픽보조모바일 코드 감지 + 서버 전송 성공 확인 (API 키 문제 해결 후)
- PC_01 에이전트 401 에러 → 삭제된 My PC API 키 무효화 → 수정
- 대시보드 원격 업데이트 배포: GitHub Release + CLI로 exe 업로드 → 배포 성공
- 원격 제어(스크린샷 + 마우스/키보드): 에이전트에 pyautogui 포함 빌드
- 에이전트 자체는 한 번 수동 교체 필요, 이후 앱은 대시보드에서 원격 업데이트
- 오프라인 PC는 자동 업데이트 안 됨 → 수동으로 다시 배포 필요 (나중에 자동화 예정)
- 보너스픽 API 분석 완료 (Network 탭 스크린샷) → API 구현
- GitHub repo public 전환 → Release 다운로드 URL 정상 작동
- 원격 업데이트 배포 최종 성공 (에이전트 앱 종료 로직 강화)
