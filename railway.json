{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "pip install fastapi uvicorn sqlalchemy pyjwt sse-starlette"
    },
    "deploy": {
        "startCommand": "uvicorn server.app:app --host 0.0.0.0 --port $PORT"
    }
}
