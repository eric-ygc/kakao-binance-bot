---
name: 세션 기록 2026-03-22
description: 스케줄러 자정 넘김 버그 수정, 로그 축소, 대시보드 비밀번호 2개 지원
type: project
---

## 2026-03-22 세션 요약

### 작업 내용
1. **스케줄러 자정 넘김 버그 수정 (v1.5.3 / v1.4.4)**:
   - 원인: `start_t <= now < stop_t` 문자열 비교 시 `23:30~00:00`이 항상 False
   - 수정: `_in_range()` 함수 추가 — 자정 넘김 시 `t >= start or t < stop`
   - API 버전 (app.py) + Selenium 버전 (.selenium-build/app.py) 둘 다 적용

2. **스케줄러 로그 축소**:
   - 기존: 24시간 1분마다 로그 (하루 1440줄)
   - 수정: 스케줄 시간 전후 5분 이내에서만 로그 출력 (하루 ~20줄)
   - `_near_schedule()` 함수 추가

3. **대시보드 비밀번호 2개 지원**:
   - `ADMIN_PASSWORD_2` 환경변수 추가 (server/config.py)
   - `verify_password()` → 사용자 라벨 반환 ("admin1" / "admin2")
   - 로그인 로그에 누가 접속했는지 기록

4. **로그 분석 결과**:
   - 07:30 스케줄: 정상 동작 확인 (자동 시작 → 코드 감지 → 자동 종료)
   - 23:30 스케줄: stop=00:00 자정 넘김 버그로 실패 → 이번 수정으로 해결
   - PC 절전모드는 아님 (전원 설정 확인됨)

### 빌드
- `dist/픽보조.exe` — API v1.5.3
- `dist/Selenium백업(v1.4)/픽보조_Selenium.exe` — Selenium v1.4.4

### 오늘 밤 테스트 계획
- Selenium 버전 + API 버전 동시 운영 (다른 PC)
- API 버전에 IPRoyal 유동IP 프록시 사용
- 계정 분리하여 각 버전에 배분

### 빌드 참고
- Selenium 빌드 진입점: **main.py** (app.py로 빌드하면 검은 화면)
- API 빌드: `pyinstaller --onefile --windowed --collect-all curl_cffi --name 픽보조 main.py`
- Selenium 빌드: `pyinstaller --onefile --windowed --name 픽보조_Selenium main.py` (.selenium-build 폴더에서)

### 미커밋 변경사항
- app.py, .selenium-build/app.py — 스케줄러 수정
- server/config.py, server/auth.py, server/routers/dashboard.py — 비밀번호 2개
- version.py — v1.5.3

**Why:** 스케줄러 자정 넘김 버그로 23:30 자동시작 실패, 대시보드 접속 분리 필요
**How to apply:** 커밋/푸시 후 Railway 자동 재배포, 다른 PC에 exe 배포
