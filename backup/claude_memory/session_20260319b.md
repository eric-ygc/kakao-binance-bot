---
name: 세션 기록 2026-03-19 오후
description: 멀티PC 관리 시스템 구축 — 웹 대시보드 + 에이전트 + Railway 배포
type: project
---

## 2026-03-19 오후 세션 요약

### 작업 내용

1. **스케줄러 독립 루프 전환**
   - 고정픽 `_check_monitor_schedule`을 `_poll_queue`에서 분리 → 자체 `self.after(10000)` 루프
   - 보너스픽과 동일한 구조로 변경
   - API + Selenium 양쪽 적용

2. **멀티PC 관리 시스템 구축**
   - **서버**: FastAPI + SQLite, Railway 배포 (`web-production-d682f.up.railway.app`)
   - **대시보드**: 비밀번호 인증, PC 관리, 설정 편집, 원격 제어, 계정 분배
   - **에이전트**: 5초 heartbeat, config 동기화, 명령 중계
   - **픽보조 연동**: status.json 기록, commands.json 감시, config 리로드

3. **대시보드 기능**
   - PC 등록/삭제, 온/오프라인 상태
   - 고정픽 + 보너스픽 계정 별도 관리
   - 엑셀(.xlsx) 직접 불러오기 (SheetJS)
   - 엑셀 내려받기 (CSV)
   - 전체 삭제, 덮어쓰기/이어쓰기 선택
   - 사이트 접속 테스트 버튼
   - 코드 수동 입력 (비상용)
   - 모니터링 원격 시작/중지

4. **설정 잠금 기능**
   - `agent_config.json` 존재 시 픽보조_Selenium 설정 읽기 전용
   - 대시보드에서만 설정 변경 가능

5. **Railway 배포**
   - 볼륨(`/data`) 연결 → DB 영구 보존
   - 환경변수: ADMIN_PASSWORD, JWT_SECRET
   - 도메인: `web-production-d682f.up.railway.app`
   - Trial → $5 결제

### 버전
- API: v1.5.1
- Selenium: v1.4.2
- 에이전트: 콘솔 모드 빌드

### 배포 파일 (다른 PC용)
```
폴더/
├── 픽보조_Selenium.exe
├── 픽보조에이전트.exe
└── agent_config.json  (server_url + api_key)
```

### 주의사항
- Railway 볼륨 연결됨 → push해도 DB 유지
- 에이전트 + 픽보조 둘 다 켜둬야 함
- 대시보드 비밀번호: ahffk!72 (사용자 설정)
