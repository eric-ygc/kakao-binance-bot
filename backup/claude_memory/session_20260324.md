---
name: 세션 기록 2026-03-24
description: config 자체저장 오인 버그 수정, 스케줄러 디버그 로그 추가, API 99계정 테스트 준비
type: project
---

## 2026-03-24 세션 요약

### 작업 내용
1. **config.json 자체 저장 오인 버그 수정**:
   - 앱이 `save_config()`로 config.json 저장 → `_check_config_reload()`가 "외부 변경"으로 오인
   - "설정이 서버에서 업데이트되었습니다" 반복 + 모니터링 중지/재시작 반복 원인
   - 수정: `_last_self_save_mtime` 변수로 자체 저장 mtime 기록 → 자체 저장이면 리로드 무시
   - API, Selenium 양쪽 모두 적용

2. **스케줄러 디버그 로그 추가**:
   - 스케줄러 재시작 조건 체크 시 `code_detected` 값 로그 출력
   - 수동 정지 후 재시작되는 원인 추적용

3. **API 버전 99계정 테스트 준비**:
   - IPRoyal 프록시 재설정 (streaming-1 모드, 요청마다 IP 변경)
   - 스케줄: 07:30~08:10, 23:30~23:59
   - 워커 2개, 에이전트 미사용 (대시보드 원격관리 안 함)

4. **Selenium 로그 분석**:
   - 07:30 자동 시작 ✓, 07:38 코드 감지 ✓, 08:10 자동 종료 ✓
   - 23:30 스케줄: config 자체저장 오인으로 모니터링 반복 중지/시작 발견 → 수정

### 버전
- API: v1.5.5
- Selenium: v1.4.5

### API 첫 실전 테스트 결과 (07:30)
- 코드 5개가 동시 감지 → 각각 99계정 자동입력 동시 실행 → 연결 오류 폭주
- 마지막 코드(1IPBHR4V8) 단독 실행 시에는 로그인/코드 제출 정상
- 원인: 모니터링 재시작 → 이전 코드 재감지 → 다중 자동입력 동시 실행

### 수정 사항 (v1.5.5)
1. `_code_detected_this_slot` 조건 없이 항상 설정 → 모니터링 재시작 방지
2. 자동입력 진행 중 다른 코드 무시 → 동시 실행 방지
3. config.json 자체저장 오인 버그 수정

### 다음 할 일
- 내일 07:30 API 99계정 재테스트
- Selenium 스케줄러 `_code_detected_this_slot` 디버그 로그 확인
- 본채트 접근성 보안 문제 해결 (다른 날)

**Why:** config 자체저장 오인이 모니터링 반복 중지/시작의 근본 원인
**How to apply:** save_config() 후 mtime 기록, _check_config_reload()에서 자체저장이면 무시
