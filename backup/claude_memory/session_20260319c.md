---
name: 세션 2026-03-19 저녁
description: 멀티PC 관리 시스템 배포 + 에이전트 commands.json 연동 수정 + Railway 볼륨 설정
type: project
---

## 작업 내용

### 스케줄러 수정 (고정픽 07:30 자동시작 문제)
- `_poll_queue` try/finally 패턴 적용 (API + Selenium 양쪽)
- `_check_monitor_schedule` 독립 루프 분리 (이전 세션에서)
- **양쪽 exe 빌드 완료** (픽보조.exe + 픽보조_Selenium.exe)

### Railway 배포
- **도메인**: web-production-d682f.up.railway.app
- **환경변수**: ADMIN_PASSWORD=ahffk!72, JWT_SECRET 설정
- **Volume**: /data 마운트 (DB 영구 보존)
- Trial → $5 결제 완료

### 에이전트 commands.json 연동 문제 해결
- **원인**: `_agent_sync_loop`가 `self.after()`로 독립 실행 시 동작 안 함 (원인 불명)
- **해결**: `_check_monitor_schedule` (스케줄러 루프) 안에 에이전트 동기화 코드 통합
- 스케줄러가 10초마다 실행되면서 commands.json도 함께 확인
- **결과**: 대시보드 → 에이전트 → commands.json → 앱 실행 흐름 정상 동작 확인

### MANAGED_MODE (서버 관리 모드)
- `agent_config.json` 존재 시 설정 입력 필드 잠금 (readonly)
- 허용 버튼: 시작, 중지, 로그, 텍스트, 위, 테스트, 접속
- 비활성 버튼/체크박스: 다크 테마 색상 유지 (흰색 X)

### 빌드 관련 주의사항
- **dist/Selenium백업(v1.4)/픽보조_Selenium.exe** ← 최신 Selenium exe 위치
- dist/ 루트의 픽보조_Selenium.exe는 이전 빌드 잔재 (삭제함)
- 사용자가 잘못된 exe를 복사하여 시간 낭비한 적 있음

### 다른 PC 설치 구성
- 픽보조_Selenium.exe + 픽보조에이전트.exe + agent_config.json (3개)
- 현재 2곳의 다른 장소 PC에서 운영 중

### API 버전 LoginFailed 수정
- `src/api_controller.py`: LoginFailed 시 다른 사이트 시도하도록 변경 (이전: 즉시 실패)
- Selenium 버전은 변경 안 함 (사용자 요청)
- 실제 동작 확인은 내일 테스트 필요

### readonly 입력필드 다크 테마
- Entry/Spinbox readonly 상태에서 `readonlybackground=C["input"]` 적용
- 비활성 버튼 `background=C["panel"]`, `foreground=C["fg_dim"]` 적용

## 다음 세션 즉시 작업 (사용자 요청)
- **관리 PC 카카오톡 코드 감지 → 대시보드 자동 전송 → 전체 PC 자동 실행**
  - 관리 PC에서 카카오톡 모니터링 → 코드 감지 → 대시보드 API로 전체 PC에 코드 전송
  - 각 PC는 카카오톡 없이도 코드를 받아서 자동 입력 실행 가능

## 미완료
- 관리 PC 카카오톡 → 대시보드 자동 코드 전송 (다음 세션 최우선)
- API 버전 자동 입력 실제 테스트 (로그 확인 필요)
- 보너스픽 계정 별도 관리 (대시보드에 추가됨, 테스트 필요)
- 엑셀 불러오기 xlsx 직접 읽기 (SheetJS 추가됨, 테스트 필요)
- 사이트 접속 테스트 버튼 (대시보드)
- 전체 PC 일괄 코드 전송 기능
- 대시보드 계정 수 표시 문제 (status.json account_count 0으로 보고)
