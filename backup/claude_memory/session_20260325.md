---
name: 세션 기록 2026-03-25
description: API 안정화 수정 (v1.5.6), 모니터링 재시작 방지, 딜레이 조정, PC간 코드 일괄 전달 계획
type: project
---

## 2026-03-25 세션 요약

### 작업 내용
1. **자동입력 완료/취소 시 `_code_detected_this_slot = True` 설정**:
   - `__auto_end__` 시점에 플래그 설정 → 모니터링 재시작 완전 차단
   - API, Selenium 양쪽 모두 적용

2. **API 로그인/코드 제출 재시도 로직 변경**:
   - 기존: 같은 계정에서 3회 재시도 (로그인), 6회 재시도 (코드 제출)
   - 변경: 실패 시 즉시 다음 계정 → 모든 계정 완료 후 실패분 일괄 재시도
   - "Frequent requests" 즉시 실패 처리 (이미 제출 성공 = 재시도 불필요)
   - Invalid parameter만 재시도 제외

3. **STAGGER_DELAY 조정**:
   - 첫 시도: 2~3초 랜덤 (`STAGGER_DELAY_API = (2.0, 3.0)`)
   - 재시도: 5초 (`STAGGER_DELAY_API_RETRY = 5.0`)
   - Selenium은 기존 10초 유지

### 버전
- API: v1.5.6
- Selenium: v1.4.5

### 향후 계획 (API 안정화 후)
- **PC간 코드 일괄 전달**: PC_01 카카오톡 코드 감지 → 서버 → 나머지 PC 일괄 전달
- 카카오톡 모니터링은 PC_01 한 대만, 나머지는 코드 수신 + 자동입력
- 대시보드에 일괄 코드 입력 버튼 추가

### 추가 수정 (3/26)
- **Frequent requests 사이트 재시도 제거**: 7개 사이트 순회 → 서버 과부하 7배 악화 원인
- **스케줄러 디버그 로그 강화**: 코드 감지 슬롯 재시작 차단 로그 추가
- **API 테스트 결과**: 71계정 중 1~4 성공, 5~7 실패(7사이트 돌림), 8 성공 → 사이트 재시도가 핵심 원인

### 다음 할 일
- API 재테스트 (Frequent requests 사이트 재시도 제거 적용 확인)
- Selenium 에러 로그 확인 (막판 1~2계정 실패 원인)
- API 안정화 확인 후 PC간 코드 일괄 전달 기능 개발

**Why:** API 버전이 모니터링 재시작 + 동시 실행 + 중간 재시도로 불안정했음
**How to apply:** 실패 즉시 넘기고 마지막 일괄 재시도, 딜레이 2~3초로 서버 과부하 방지
